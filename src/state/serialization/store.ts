// Re-export top-level serialization functions from the original file
export {
  normalizeResources,
  normalizeModules,
  normalizePrestige,
  normalizeSave,
  normalizeNotation,
  normalizePerformanceProfile,
  normalizeSettings,
  normalizeSnapshot,
  serializeStore,
  stringifySnapshot,
  parseSnapshot,
} from '../serialization';
